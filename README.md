# CCDEVAP-MP1 (Phase 2)

How to run the site:
1. Enable Apache and MySQL in XAMPP
2. Make sure the database is imported.
3. Change the values in ../config/connections.php to match your MySQL information, along with the database name.
4. Open the webpage, it should be "http://localhost:3000"
